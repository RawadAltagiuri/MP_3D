clc, clear
load envs

% inputs: sp, number of iterations in each combination, number of combinations that would be tested

% sp = envs{3};
% sp.baseRotate = false;
run_num = 10;

% sp = envs{1};
% tabulation(sp, run_num);


% SAAT: 15.45 06/08/2024, GÜNLERDEN SALI


for i=1:size(envs, 2)
    sp_counter = i      
    sp = envs{i};
    tabulation(sp, run_num);
end